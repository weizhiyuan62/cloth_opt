defaults:
  - env: default
  - policy: symmetric_fold
  - optimizer: cem
  - render: disabled
  - _self_

seed: 0

env:
  reset_height: 0.0
  n_substeps: 5
  scene:
    width: 10
    height: 10
    spacing: 0.1

hydra:
  run:
    dir: outputs/optimize/${now:%Y-%m-%d}/${now:%H-%M-%S}
  job:
    chdir: true
  job_logging:
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        level: INFO
        stream: ext://sys.stdout
      file:
        class: logging.FileHandler
        formatter: simple
        level: INFO
        filename: ${hydra.runtime.output_dir}/${hydra.job.name}.log
    root:
      level: INFO
      handlers: [console, file]
