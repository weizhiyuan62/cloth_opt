enabled: true
backend: polyscope
keep_frames: false
fps: 50
# auto tries GLFW first and EGL on a display-less Linux server.
# Use egl or glfw to require one backend explicitly.
engine: auto
image_width: 960
image_height: 720
material: wax
ground_plane: shadow_only
camera:
  elevation: 28.0
  azimuth: 45.0
  distance_scale: 1.0
